#!/usr/bin/env bash
set -e

# Kill all background processes when this script exits (Ctrl+C or error)
trap 'kill 0' EXIT

echo "==> Starting Rails server..."
bin/rails server -p 3000 &

echo "==> Starting Solid Queue worker..."
bin/jobs &

wait
